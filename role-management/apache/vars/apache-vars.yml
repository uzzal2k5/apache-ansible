PACKAGE:
  - apache2
APACHE_VERSION: "latest"
SUBDOMAIN: 'web'
HTTP_HOST: "{{DOMAIN_NAME}}"
HTTP_PORT: "80"
HTTPS_PORT: "443"
CONTENT_ROOT: "/var/www/{{ CONTENT_ROOT }}"
CERTIFICATE_ROOT: "/etc/ssl/certs"
disable_default: true

